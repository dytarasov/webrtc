worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        server_name strangerstolk.ru www.strangerstolk.ru;

        # Путь для Let's Encrypt проверки
        location /.well-known/acme-challenge/ {
            root /usr/share/nginx/html;  # Используем директорию, где хранятся временные файлы для проверки
            try_files $uri =404;  # Возвращаем 404, если файл не найден
        }

        # Обслуживание статики (HTML, JS, CSS)
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        # Проксирование запросов на бэкенд (API)
        location /api/ {
            proxy_pass http://127.0.0.1:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Проксирование WebSocket
        location /ws/ {
            proxy_pass http://127.0.0.1:8000/ws/;  # Проксируем WebSocket на сервер FastAPI
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name strangerstolk.ru www.strangerstolk.ru;

        ssl_certificate /etc/letsencrypt/live/strangerstolk.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/strangerstolk.ru/privkey.pem;

        # Обслуживание статики
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        # Проксирование API запросов
        location /api/ {
            proxy_pass http://127.0.0.1:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Проксирование WebSocket для работы через SSL
        location /ws/ {
            proxy_pass http://127.0.0.1:8000/ws/;  # Проксируем WebSocket
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
